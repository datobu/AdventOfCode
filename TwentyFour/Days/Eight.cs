namespace TwentyFour.Days;

internal class Eight
{
    public int Run()
    {
        PartOne();

        // PartTwo();

        return PartOne();
    }

    private readonly int _count = 0;

    public int PartOne()
    {
        // var input = File.ReadAllLines("../../../Common/Inputs/DayEight.txt");

        // Create Matrix

        // Copy Matrix to 'solution Matrix'

        // Run through Matrix, until one char != . and char != '#' is reached
        // store this coordinate and scan the map for all other coordinates with this same char in a list
        // based on the coordinates calculate where all antinodes are to be placed
        // place the anti nodes, only when they are on a '.' field, mark them with '#' in the solution Matrix

        // loop until end of input

        // loop through solution Matrix and Count '#' chars.

        return _count;
    }
}